/* eslint-disable react/no-unescaped-entities */
import "./style.scss";

export const Rating = () => {

  
  return (
    <div className="rating-container">
      <button onClick={}>Actually bad</button>
      <button onClick={}>So bad it's good</button>
      <button onClick={}>Meh, not worth it</button>
      <button onClick={}>Worth a watch</button>
      <button onClick={}>It's actually great</button>
    </div>
  );
};
